{
  "name": "workflow-node",
  "version": "4.0.1-beta.15",
  "description": "Community maintained Node server helpers for Vercel Workflow DevKit",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publish-branch": "bun-node-support",
  "publishConfig": {
    "access": "public"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/eersnington/workflow-cloudflare-world.git",
    "directory": "packages/node"
  },
  "exports": {
    ".": "./dist/index.js",
    "./builder": "./dist/builder.js",
    "./server": "./dist/server.js",
    "./manifest": "./dist/manifest.js",
    "./workflows": "./dist/workflows.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "clean": "tsc -p tsconfig.json --build --clean && rm -rf dist",
    "test": "vitest run src"
  },
  "dependencies": {
    "@workflow/builders": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@workflow/tsconfig": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
