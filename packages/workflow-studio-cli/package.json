{
  "name": "workflow-studio",
  "version": "0.1.5-alpha",
  "description": "Devtools for Vercel Workflow DevKit",
  "type": "module",
  "bin": {
    "workflow-studio": "./dist/index.js",
    "studio": "./dist/index.js"
  },
  "files": [
    "dist",
    "templates",
    "src/handlebars"
  ],
  "scripts": {
    "build": "tsc && chmod +x dist/index.js",
    "clean": "tsc --build --clean && rm -rf dist",
    "dev": "tsc --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ast-grep/napi": "0.40.0",
    "@clack/prompts": "^0.11.0",
    "@workflow/cli": "workspace:*",
    "glob": "12.0.0",
    "handlebars": "4.7.8",
    "picocolors": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@workflow/tsconfig": "workspace:*",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "Apache-2.0",
  "publish-branch": "bun-node-support",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/eersnington/workflow-cloudflare-world.git",
    "directory": "packages/workflow-studio-cli"
  }
}
